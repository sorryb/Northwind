@model NorthwindWeb.Models.Order_Details

@{
    ViewBag.Title = "Modifica";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}



    <h2>Modifica</h2>
    <hr />
    <ol class="breadcrumb">
        <li>
            @Html.ActionLink("Dashboard", "Home", "Dashboard")
        </li>
        <li>
            @Html.ActionLink("Comenzi", "Index", "Orders")
        </li>
        <li>
            @Html.ActionLink("Detalii", "Details", "Orders", new { id = Model.OrderID }, null)
        </li>
        <li class="active">Modifica</li>
    </ol>

    @using (Html.BeginForm("Edit", "OrderDetail", FormMethod.Post, htmlAttributes: new { @class = "ord-form" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>Detalii comanda</h4>
            <hr />
            @Html.ValidationSummary(true)
            @Html.HiddenFor(model => model.OrderID)

            @Html.HiddenFor(model => model.ProductID)

            <div class="form-group">
                <span class="control-label col-md-2 bold"><strong>Cantitate</strong></span>
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Quantity)
                    @Html.ValidationMessageFor(model => model.Quantity)
                </div>
            </div>

            <div class="form-group">
                <span class="control-label col-md-2 bold"><strong>Discount</strong></span>
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Discount)
                    @Html.ValidationMessageFor(model => model.Discount)
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Salveaza" class="btn btn-default" />
                </div>
            </div>
        </div>
    }

    <div>
            @Html.ActionLink("Inapoi", "Details", "Orders",new { id = Model.OrderID }, new { @class = "btn btn-default" })

    </div>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }


