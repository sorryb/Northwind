@model NorthwindWeb.Models.Territories

@{
    ViewBag.Title = "Sterge";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

    <h2>Sterge</h2>
    <hr />
    <ol class="breadcrumb">
        <li>
            @Html.ActionLink("Dashboard", "Home", "Dashboard")
        </li>
        <li>
            @Html.ActionLink("Regiuni", "Index", "Regions")
        </li>
        <li>
            @Html.ActionLink("Detalii", "Details", "Regions", new { id = Model.RegionID }, null)
        </li>
        <li class="active">Sterge</li>
    </ol>

    <h3>Sigur vrei sa stergi?</h3>
    <div>
        <h4>Teritorii</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                Descriere regiune<br />
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Region.RegionDescription)<br />
            </dd>

            <dt>
               Descriere teritoriu<br />
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TerritoryDescription)<br />
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                @Html.ActionLink("Inapoi", "Details", "Regions", new { id = Model.RegionID }, new { @class = "btn btn-default" })
                <input type="submit" value="Sterge" class="btn btn-default" />
            </div>
        }
    </div>


