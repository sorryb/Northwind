@model PagedList.IPagedList<NorthwindWeb.Models.Products>
@using PagedList.Mvc;
@{int itemCount = Model.Count; }
<div class="container">

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Produse
            </h1>
            <div>
                <ol class="breadcrumb">
                    <li class="active">
                        @ViewBag.title

                        <form id="searchInput" class="prodsearch" action="@Url.Action("Products","Products",new { category=ViewBag.title, search,page=1 } )">
                            <div class="input-group">
                                <input type="text" name="search" class="form-control searchinput" style="width:auto" placeholder="Search">
                                <div class="input-group-btn">
                                    <button class="btn btn-default" type="submit">
                                        <i class="glyphicon glyphicon-search"></i>
                                    </button>
                                </div>
                            </div>
                        </form>
                    </li>
                </ol>
            </div>
        </div>
    </div>
    @{
        for (int i = 0; i < itemCount; i += 2)
        {
            <div class="row productdiv">
                <div class="col-md-6 portfolio-item">
                    <img src="/Images/@(ViewBag.title)/@(Model.ElementAt(i).ProductID).jpg" class="img-responsive contentcenter products whiterounded">

                    <h3 class="text-center prodpad">
                        @Model.ElementAt(i).ProductName <br />
                        <span class="price"> @Decimal.Round((decimal)Model.ElementAt(i).UnitPrice, 2) Lei</span>
                    </h3>
                </div>
                @if (i + 1 < itemCount)
                {
                    <div class="col-md-6 portfolio-item">

                        <img src="/Images/@(ViewBag.title)/@(Model.ElementAt(i+1).ProductID).jpg" class="img-responsive contentcenter products whiterounded">
                        
                        <h3 class="text-center prodpad">
                            @Model.ElementAt(i + 1).ProductName <br />
                            <span class="price"> @Decimal.Round((decimal)Model.ElementAt(i + 1).UnitPrice, 2) Lei</span>
                        </h3>
                    </div>
                }
            </div>
            <hr />
        }
    }
    <hr>
    <div class="row text-center">
        <div class="col-lg-12">
            Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

            @Html.PagedListPager(Model, page => Url.Action("Products", "Products",
            new { category = ViewBag.title, page }))
        </div>
    </div>
</div>